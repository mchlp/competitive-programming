#include <bits/stdc++.h>

using namespace std;

int N, M;
bool starting[100001];
vector<pair<int, int>> edges;

int main() {

    memset(starting, true, sizeof starting);
    scanf("%d%d", &N, &M);
    for (int i=0; i<M; i++) {
        int a, b;
        scanf("%d%d", &a, &b);
    }

    return 0;
}